{% block body %}

    <section class="row justify-content-center">
        <div class="col-8 text-center">
            <h1 class="display-4">{{ test.name }}</h1>
            <p class="lead">Cette épreuve est composée de {{ itemsCount }} items actifs</p>
        </div>
    </section>

{% endblock %}
